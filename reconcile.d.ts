export default function reconcile(
    parent: HTMLElement,
    renderedValues: any[],
    data: any[],
    createFn: Function,
    noOp?: Function,
    beforeNode?: Node,
    afterNode?: Node): void;
